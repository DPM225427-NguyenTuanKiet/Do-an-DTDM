@model CarShop.Models.ThuocTinh
@{
    ViewData["Title"] = "Thêm thuộc tính";
    Layout = "_AdminLayout";
}

<div class="card shadow-sm">
    <div class="card-header bg-success text-white">
        <h4><i class="fas fa-plus"></i> Thêm thuộc tính mới</h4>
    </div>
    <div class="card-body">
        <form asp-action="Create" method="post">
            <div class="mb-3">
                <label asp-for="TENTHUOCTINH" class="form-label">Tên thuộc tính</label>
                <input asp-for="TENTHUOCTINH" class="form-control" required />
            </div>
            <button type="submit" class="btn btn-orange"><i class="fas fa-save"></i> Lưu</button>
            <a asp-action="Index" class="btn btn-secondary">Hủy</a>
        </form>
    </div>
</div>